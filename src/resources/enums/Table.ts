/**
 * List of database tables.
 */
enum Table {
  USERS = 'users',
  USER_ROLES = 'roles',
  USER_SESSIONS = 'user_sessions',

  VEHICLES = 'vehicles',
  RIDES = 'rides',
  RIDE_LOGS = 'ride_logs',
}

export default Table;
